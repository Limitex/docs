import Image from "next/image";
import { Tabs, Cards } from "nextra/components";
import { Box, Package } from "lucide-react";
import Tree from "../../../components/tree.tsx";
import ComponentsTabs from "../../../components/components-tabs.tsx";
import Switch from "../../../components/unity-hierarchy/switch.tsx";

# Switch

Displays a switch or a component that looks like a switch.

<ComponentsTabs component={Switch.Default()} />

## Installation

Not required.

## Usage

<Tree.Container>
  <Tree.Root defaultExpanded>
    <Tree.Item name="Canvas" type={Box}>
      {Switch.Default({ defaultExpanded: false }).content}
      {Switch.Default({ defaultExpanded: false }).content}
    </Tree.Item>
  </Tree.Root>
</Tree.Container>

## Examples

### Primary

<ComponentsTabs component={Switch.Default()} />
