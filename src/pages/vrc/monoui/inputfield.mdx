import Image from "next/image";
import { Tabs, Cards } from "nextra/components";
import { Box, Package } from "lucide-react";
import Tree from "../../../components/tree.tsx";
import ComponentsTabs from "../../../components/components-tabs.tsx";
import InputField from "../../../components/unity-hierarchy/inputfield.tsx";

# InputField

Displays a inputField or a component that looks like a inputField.

<ComponentsTabs component={InputField.Default()} />

## Installation

Not required.

## Usage

<Tree.Container>
  <Tree.Root defaultExpanded>
    <Tree.Item name="Canvas" type={Box}>
      {InputField.Default(false).content}
      {InputField.Default(false).content}
    </Tree.Item>
  </Tree.Root>
</Tree.Container>

## Examples

### Primary

<ComponentsTabs component={InputField.Default()} />
