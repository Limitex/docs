import Image from "next/image";
import { Tabs, Cards } from "nextra/components";
import { Box, Package } from "lucide-react";
import Tree from "../../../components/tree.tsx";
import ComponentsTabs from "../../../components/components-tabs.tsx";
import Toggle from "../../../components/unity-hierarchy/toggle.tsx";

# Toggle

Displays a toggle or a component that looks like a toggle.

<ComponentsTabs component={Toggle.Default()} />

## Installation

Not required.

## Usage

<Tree.Container>
  <Tree.Root defaultExpanded>
    <Tree.Item name="Canvas" type={Box}>
      {Toggle.Default({ defaultExpanded: false }).content}
      {Toggle.Outline({ defaultExpanded: false }).content}
      {Toggle.Group({ defaultExpanded: false }).content}
      {Toggle.Radio({ defaultExpanded: false }).content}
      {Toggle.RadioGroup({ defaultExpanded: false }).content}
    </Tree.Item>
  </Tree.Root>
</Tree.Container>

## Examples

### Toggle

<ComponentsTabs component={Toggle.Default()} />

### Toggle Outline

<ComponentsTabs component={Toggle.Outline()} />

### Toggle Group

<ComponentsTabs component={Toggle.Group()} />

### Radio Toggle

<ComponentsTabs component={Toggle.Radio()} />

### Radio Toggle Group

<ComponentsTabs component={Toggle.RadioGroup()} />
